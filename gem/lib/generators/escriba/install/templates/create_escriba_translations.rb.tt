class CreateEscribaTranslations < ActiveRecord::Migration[<%= ActiveRecord::Migration.current_version %>]
  def change
    create_table :escriba_translations do |t|
      t.string  :key,                 null: false, limit: 32
      t.string  :locale,              null: false, limit: 16
      t.text    :value
      t.text    :source_copy,         null: false
      t.text    :meaning
      t.text    :interpolation_names
      t.boolean :plural,              null: false, default: false
      t.text    :issues # lint issues cached at write time (JSON array), NULL when clean
      t.timestamps
    end

    add_index :escriba_translations, [:key, :locale], unique: true
    add_index :escriba_translations, :key
    add_index :escriba_translations, :locale, where: "issues IS NOT NULL",
      name: "index_escriba_translations_issue_rows"
  end
end
